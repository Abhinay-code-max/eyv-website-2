# Direct dependencies only - pip resolves the real transitive tree from
# these. This used to be a raw `pip freeze` (170+ packages, including things
# like pandas/numpy/boto3/tiktoken with zero references anywhere in this
# app - leftovers from the base image this project was originally
# provisioned from). Trimmed to what server.py/services/*.py/tests/*.py
# actually import, verified by grep, plus the handful of runtime
# dependencies FastAPI/uvicorn need that aren't themselves `import`ed
# (uvicorn is invoked as a process, python-multipart backs UploadFile/File
# parsing for the wallet-upload endpoint).
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0
watchfiles==1.2.0
python-multipart==0.0.30

motor==3.3.1
pymongo==4.5.0

pydantic==2.13.4

google-genai==2.7.0
stripe==14.4.1
sentry-sdk==2.66.0

httpx==0.28.1
requests==2.34.2
slowapi==0.1.10

python-dotenv==1.2.2
python-json-logger==4.1.0
APScheduler==3.11.3
Pillow==12.2.0

# Testing - backend/tests/ and the CI jobs that run it
pytest==9.0.3
